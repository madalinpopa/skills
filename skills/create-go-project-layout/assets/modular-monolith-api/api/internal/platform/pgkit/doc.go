// Package pgkit holds the Postgres helpers every module uses: migrations for
// one module schema, transactions with retry, unique violation checks, and a
// Testcontainers database for integration tests.
//
// Placeholder: a platform component skill fills this package.
package pgkit
