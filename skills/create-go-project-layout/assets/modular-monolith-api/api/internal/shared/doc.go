// Package shared holds the types every module shares, such as the UUID
// value type built on the standard library uuid package.
//
// Placeholder: a platform component skill fills this package.
package shared
