# Web

Placeholder for the frontend of `{{PROJECT_NAME}}`. A frontend component skill
scaffolds it. It is served separately from the API and talks JSON over HTTP,
with the API base URL read from `VITE_API_BASE_URL`.
